const message = () => {
    const name = "Kyle";
    const age = 24;
    return name + 'is' + age 'years old.';
};

export default message;

// We can import modules into files in two ways depending on if they are default exports and named exports.
// Named Exports are constructed using curly braces, default exports are not.




